fastapi>=0.109.0
uvicorn[standard]>=0.27.0
jinja2>=3.1.0
python-multipart>=0.0.9

numpy>=1.24.0
opencv-python-headless>=4.8.0
mediapipe==0.10.18
psutil

# PyTorch resolves to CPU-only wheels on Raspberry Pi OS / Linux aarch64.
torch>=2.0.0

# Intel RealSense is required for the runtime UI.
# On Raspberry Pi 5 / Linux aarch64 there is no normal PyPI wheel for pyrealsense2.
# Install librealsense and its Python bindings manually before launching the UI.

# TensorFlow Lite runtime for MobileFaceNet on Raspberry Pi OS.
ai_edge_litert
