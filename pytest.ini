[pytest]
markers =
    integration: tests requiring optional runtime dependencies and real image fixtures

# Without this, LOG.info from integration tests (embedding, alignment, detection) does not
# appear on the terminal. Disable locally with: pytest -o log_cli=false
log_cli = true
log_cli_level = INFO
log_cli_format = %(levelname)s %(name)s: %(message)s
